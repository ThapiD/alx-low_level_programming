#include "main.h"
/**
 * _islower - Checks if input is a lowercase character
 * @c: character to be checked
 *
 * Return: Always 1 for lowercase character, 0 for otherwise
 */
int _islower(int c)
{

	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	else if 
	{	
		return (0);
	}
}
