#include "main.h"
/**
 * _islower - Shows 1 if input is a lowercase character
 * Another case shows 0
 *
 * Return: Always 1(success) for lowercase character, 0 for otherwise
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	else if 
	{
		return (0);
	}
	_putchar('\n');
}
