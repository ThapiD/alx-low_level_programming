#include "main.h"
/**
 * mul - multiplies two integers
 * @a: check for integer
 * @b: check fr integer
 * Return: 0 multiplication of integers(success)
 */
int mul(int a, int b)
{
	return (a * b);
}
