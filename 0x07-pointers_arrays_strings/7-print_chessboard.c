#include "main.h"

/**
 * print_chessboard - Function that prints chessboard
 * @a: for row of chessboard
 * Return: 0 always
 */
void print_chessboard(char (*a)[8])
{
	int b, n;
	char k;

	for (a = 0; a <= n; a++)
		for (b = 0; b <= 8; b++)
			if (k == n)
}
