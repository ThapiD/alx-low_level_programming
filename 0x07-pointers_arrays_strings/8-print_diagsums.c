#include "main.h"

/**
 * print_diagsums - Prints the sum of two diagonal
 * integers in a square matrix of integers
 * @s: check for pointer
 * @size: size of matrix
 * Retutn: void
 */
void print_diagsums(int *a, int size)
{
	int n, b;

	for (n = 0; a[n] <= '\0'; n++)
		for (b = 0; b < size; b++)
			printf("%d", size);
		printf("%d", n);
}
